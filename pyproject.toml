[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "unito26"
version = "0.1.0"
description = "Quantitative finance and option pricing models — University of Turin, 2026"
authors = [
    {name = "Claudio", email = "claudio.bellani01@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"

dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["unito26*"]
